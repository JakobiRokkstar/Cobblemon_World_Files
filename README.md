# Cobblemon_World_Files

Different world files to use for the server

### Upload Local World → Server

Push local world changes to the server:

```bash
./scripts/upload_pokemon_world.sh
```

---

### Download Server World → Local

Fetch the latest world data from the server:

```bash
./scripts/download_pokemon_world.sh
```
